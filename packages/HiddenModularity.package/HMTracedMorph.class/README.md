A HMTracedMorph is a normal morph that traces each execution of #drawOn: and exports all the trace graphs.

Instance Variables
	traceCounter:		<Integer>

traceCounter
	- counts the current trace in order to name the export file accordingly
